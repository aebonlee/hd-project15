# [Claude Code 작업 지시서] Facility AI — 유틸리티 설비·에너지 통합 관리

- 기획자: 지준경
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 15번
- 프로젝트 유형: 설비 대장 + 법정검사·소모품 일정 계산 + 비용 예측 + 고지서 파싱
- 대체 대상: **캠퍼스 유틸리티 설비를 엑셀과 기억에 의존해 관리하던 일**

> ⚠ **이 문서는 기획서 원문이 아니라 저장소에서 되짚어 쓴 것이다.**
> 다른 프로젝트의 작업 지시서는 기획서를 먼저 받아 적은 것이지만, 이 저장소는
> 구현이 먼저 있었고 지시서가 없었다. 그래서 §1~§5 는 **README·커밋 메시지·
> 테스트에서 실제로 확인한 것만** 옮겼고, **부록 A(기획서 원문)는 비워 두었다.**
> 지어내면 다음 사람이 그것을 기획자의 말로 믿는다 — 이 도구가 검사 주기를
> 지어내지 않는 것과 같은 이유다.

---

## 0. Claude Code에게 — 작업 방식

1. 이 도구가 다루는 것은 **법정검사 기한**이다. 이틀 밀린 것을 화면에서
   알아채기는 거의 불가능한데, 이 시스템에서 이틀은 "기한을 넘겼는가"를 가른다.
   **날짜 계산은 눈으로 검산할 수 없으므로 반드시 테스트로 고정한다.**
2. 규칙은 `js/schedule.js`(날짜) · `js/law.js`(법령) · `js/energy.js`(고지서)에
   순수 함수로 모으고 `test/logic.test.js` 로 고정한다. 화면은 판정하지 않는다.
3. **페이지를 손으로 고치지 않는다.** `build.py` 가 일곱 장을 굽고 덮어쓴다.
   본문은 `PAGES`, 머리말·메뉴는 `HEAD`/`MENU` 에 있다.
4. **바깥을 부르지 않는다.** 사내 폐쇄망에서 돌아야 한다 — 필요한 파일은
   전부 저장소에 넣는다(§3.3).
5. **사내 실데이터를 저장소에 넣지 않는다.** 「예시 자료 넣기」가 만드는
   가짜 설비·이력·고지서를 쓴다.

---

## 1. 한 줄 요약

캠퍼스 유틸리티 설비를 **대장으로 관리**하고, **법정검사·소모품 교체 시기를 계산**하고,
이력과 금액을 쌓아 **차년도 비용을 예측**하고, 월별 **에너지 고지서에서 사용량을 뽑는**
일곱 화면짜리 도구.

## 2. 배경 및 현재 업무 상황

- 캠퍼스 안 유틸리티 설비(보일러·승강기·공조 등)를 한 사람이 여러 대 관리한다.
- 설비마다 **법정검사 주기가 다르고**, 소모품 교체 시기도 따로 돈다.
- 지금은 엑셀과 기억에 의존한다. 기한을 놓치면 그건 실제 피해다.
- 이력과 금액이 흩어져 있어 **차년도에 얼마가 필요한지** 미리 알기 어렵다.

## 3. 저장소에서 확인한 것

### 3.1 화면 일곱

| 화면 | 하는 일 |
|---|---|
| 개요 | 지금 챙길 것 — 기한 초과·임박·확인 필요 |
| 설비 | 사양·담당자·관련 법령 등록, 엑셀/JSON 내보내기 |
| 알림 | 검사·교체 시기 계산, 담당자별 알림 문안 만들기 |
| 이력 | 교체·고장 AS·법정검사 기록과 금액, 설비별 누계 |
| 비용 | 차년도에 돌아오는 항목과 금액 |
| 에너지 | 고지서 PDF·엑셀에서 사용량 추출 → 그래프 → 엑셀 |
| 조감도 | 건물을 눌러 그 건물 설비 보기 |

### 3.2 기획서에 있었지만 **일부러 넣지 않은 것 둘**

못 해서가 아니라 **넣으면 안 되기 때문**이다.

**① 검사 주기를 알려 주지 않는다.**
같은 「보일러」라도 용량·종별·설치 장소에 따라 법정검사 주기가 다르다.
숫자 하나를 미리 박아 두면 **그럴듯해 보이고, 그래서 아무도 다시 확인하지 않는다.**
틀린 주기로 법정검사를 놓치면 실제 피해다.
→ 설비 종류를 고르면 **관련 법령 이름과 국가법령정보센터 검색 링크**만 준다.
주기가 비면 「주기 없음」으로 표시되고 개요의 **확인 필요**에 올라온다.

**② 법령 자동조회·메일 자동발송을 넣지 않았다.**
둘 다 서버와 인증키가 필요하고 **사내 폐쇄망에서는 애초에 나갈 수 없다.**
→ 법령 개정 모니터링 대신 **「확인이 필요한 것」 목록**을 뽑고,
메일 자동발송 대신 **담당자별 알림 문안**을 만들어 준다.
폐쇄망에서도 돌고, 틀린 법령 정보가 자동으로 퍼질 위험도 없다.

### 3.3 폐쇄망에서 돌아간다

바깥을 부르는 곳이 **한 군데도 없다.**

| 필요한 것 | 어떻게 |
|---|---|
| 엑셀 읽기·쓰기 | `lib/xlsx.full.min.js` 동봉 |
| PDF 글자 뽑기 | `lib/pdf.min.mjs` + `lib/cmaps/` (한글 PDF 는 cMap 이 없으면 못 읽는다) |
| 그래프 | 라이브러리 없이 **SVG 로 직접** 그린다 |
| 글꼴 | OS 기본 글꼴 |

확인은 **localhost 말고 전부 차단한 크롬**으로 한다 —
`node ../hd-04/scripts/probe.mjs /hd-project15/ …`. 거기서 뜨면 폐쇄망에서도 뜬다.

### 3.4 자료는 이 브라우저에만 있다

localStorage 다. 서버가 없다. 다른 PC 로 옮기려면 **내보내기(JSON) → 가져오기**.
브라우저 기록을 지우면 자료도 사라지므로 주기적으로 내보내야 한다.
**여러 사람이 같이 쓰려면 서버가 필요하다 — 지금 구조로는 안 된다.**

---

## 4. 반드시 규칙으로 막을 것 (테스트로 고정)

`test/logic.test.js` 61개가 지킨다. ★ 는 틀리면 **기한을 놓치거나 예산이 어긋나는** 것.

| # | 규칙 | 어디서 |
|---|---|---|
| 1 | ★ **말일을 넘기지 않는다** — 1/31 + 1개월은 3/3 이 아니다 | `Schedule.addMonths` |
| 2 | ★ 마지막 검사일이 없으면 **날짜를 지어내지 않는다.** 「이력 없음」 | `Schedule.nextInspection` |
| 3 | ★ 비용 미상은 **합계에서 빼고** 「셀 수 없었던 것」에 이유와 함께 남긴다 | `Schedule.forecastYear` |
| 4 | ★ 법령표에 **주기를 지어내지 않는다** — 법령명과 검색 링크만 | `Law.lawsFor` |
| 5 | 13월은 없다 · 날짜 표기가 여러 가지여도 읽는다 | `Schedule.parseDate` |
| 6 | 한 해에 여러 번 오는 주기를 한 번으로 세지 않는다 | `Schedule.forecastYear` |
| 7 | 법령 미확인·주기 미상·사양 없음을 골라내고 **왜 골랐는지 적는다** | `Law.needsReview` |
| 8 | 고지서에서 **못 읽은 달은 행을 만들지 않는다** + 몇 줄을 못 읽었는지 알린다 | `Energy.parseUsage` |
| 9 | 천단위 쉼표·`YYYY-MM`·요금 표기를 모두 읽되 **단위는 그대로 둔다** | 같음 |

### 모르는 값을 0 으로 채우지 않는다

이 도구가 가장 신경 쓴 부분이다.

| 상황 | 흔한 처리 | 여기서는 |
|---|---|---|
| 이력 금액을 모름 | 0 원 | **미상** — 합계에서 빼고 건수로 따로 |
| 비용 예측에 단가 없음 | 0 원으로 합산 | **「셀 수 없었던 것」** 에 이유와 함께 |
| 마지막 검사일 없음 | 오늘 기준 계산 | **「이력 없음」** — 날짜를 지어내지 않음 |
| 고지서에서 못 읽은 달 | 0 으로 그래프 바닥 | **행을 만들지 않음** |

0 으로 채우면 합계가 멀쩡해 보이고 **어디에도 표가 나지 않는다.**
예산을 실제보다 적게 잡거나, "3월에 전기를 안 썼다"는 그래프를 보고
원인을 찾으러 다니게 된다.

---

## 5. 산출물

- 빌드는 `build.py` 가 페이지 일곱 장을 굽는다 (GitHub Pages 배포)
- `test/logic.test.js` 61개 — 날짜·법령·에너지 파싱
- `test/smoke.browser.js` 25개 — 브라우저에 일곱 장을 실제로 띄운다
- 라이브러리 동봉(`lib/`) — 폐쇄망 전제
- `README.md`, `docs/개발일지.md`

---

## 부록 A. 기획서 원문

> **비어 있음.**
>
> 이 저장소에는 기획자(지준경)의 기획서 원문이 남아 있지 않다.
> 다른 프로젝트의 작업 지시서는 이 자리에 원문을 그대로 싣고 있고,
> 그 원문이 최종 기준 역할을 한다.
>
> 다만 README 에 **원문에서 온 것이 분명한 요구 두 가지**가 남아 있다 —
> 「법령 개정 모니터링」과 「알림 메일 발송」. 둘 다 그대로 구현하지 않고
> 폐쇄망에서 돌 수 있는 형태로 바꿨다(§3.2). **왜 바꿨는지가 기록되어
> 있으므로 원문 없이도 그 판단은 되짚을 수 있다.**
>
> **채우는 방법**: 원문을 받아 이 아래에 인용(`>`)으로 그대로 붙인다.
> 붙인 뒤 §1~§4 가 원문과 어긋나는 곳이 없는지 확인한다 —
> 지금 적힌 것은 구현에서 되짚은 것이라, **기획자가 요구했지만 구현되지 않은
> 항목은 여기 없을 수 있다.**
>
> 요약하거나 다시 쓰지 말 것. 원문 그대로가 기준이다.
